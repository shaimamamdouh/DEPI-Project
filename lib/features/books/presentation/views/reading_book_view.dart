import 'package:flutter/material.dart';

class ReadingBookView extends StatelessWidget {
  const ReadingBookView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
